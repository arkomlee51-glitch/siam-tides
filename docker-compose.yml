# Redis สำหรับพัฒนาในเครื่อง
# Supabase รันแยกด้วย Supabase CLI: `npx supabase start` (ดู README)
services:
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    command: ['redis-server', '--appendonly', 'yes']
    volumes:
      - redis-data:/data
volumes:
  redis-data:
